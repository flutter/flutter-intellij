include: package:lints/recommended.yaml

analyzer:
  strong-mode:
    implicit-casts: false
  exclude:
    - 'out/**'
    - 'tool/icon_generator/**'

linter:
  rules:
    - directives_ordering
    - unawaited_futures
