name: plugin_tool
description: Flutter plugin tool.
version: 0.0.1

environment:
  sdk: '>=2.0.0-dev <3.0.0'

dependencies:
  args: ^1.0.0
  cli_util: ^0.1.0
  git: ^0.5.1
  markdown: ^2.0.0
  path: ^1.6.2

dev_dependencies:
  test: ^1.5.0
